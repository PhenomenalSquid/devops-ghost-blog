name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2


#    - name: build environment
#      run: |
#        echo "GHOST_DBPW=${{ secrets.GHOST_DBPW }}" >> .env
#        echo "GHOST_DBUSER=${{ secrets.GHOST_DBUSER }}" >> .env
      
    - name: Build and test with Docker Compose
      run: |
        env | grep -i ghost
        docker-compose up -d db
        sleep 30 #make sure db is up and running first
        docker-compose up -d ghost
        docker-compose ps
        sudo docker-compose ps | grep ghost | grep -i up
        sudo docker-compose ps | grep db | grep -i up
        docker-compose down
      env:
        MYSQL_PASSWORD: ${{ vars.GHOST_DBPW }}
        MYSQL_ROOT_PASSWORD: ${{ vars.GHOST_DBPW }}
        MYSQL_DBUSER: ${{ vars.GHOST_DBUSER }}
        MYSQL_PASSWORD: ${{ vars.GHOST_DBPW }}
  
    #- name: Diagnose MySQL logs
    #  if: always()
    #  run: |
    #       docker logs devops-ghost-blog_db_1
    #       echo "${{ secrets.GHOST_DBPW }}"
    #       echo "GHOST_DBUSER=${GHOST_DBUSER}"
    #       echo "MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}"
    #       echo "MYSQL_PASSWORD=${MYSQL_PASSWORD}"

